import { Container } from '@material-ui/core';
import React from 'react';
// import PropTypes from 'prop-types';

const Home = () => {
  return (
    <Container>
      Homepage
    </Container>
  );
};
export default Home;
