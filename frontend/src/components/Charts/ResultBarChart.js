import React from 'react';
import PropTypes from 'prop-types';

const ResultBarChar = ({ data }) => {
  return (
    <></>
  );
};

ResultBarChar.propTypes = {
  data: PropTypes.array
};

export default ResultBarChar;
